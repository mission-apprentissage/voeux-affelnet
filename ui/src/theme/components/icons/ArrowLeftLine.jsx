import { Icon } from "@chakra-ui/icons";

import { ReactComponent as ArrowLeftLineIcon } from "../../assets/arrow-left-line.svg";

export const ArrowLeftLine = (props) => {
  return <Icon as={ArrowLeftLineIcon} {...props} />;
};
