import { Icon } from "@chakra-ui/icons";

import { ReactComponent as FileDownloadLineIcon } from "../../assets/file-download-line.svg";

export const FileDownloadLine = (props) => {
  return <Icon as={FileDownloadLineIcon} {...props} />;
};
