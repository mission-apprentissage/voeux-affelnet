import { Icon } from "@chakra-ui/icons";
import { ReactComponent as CheckLineIcon } from "../../assets/arrow-right-line.svg";

export const CheckLine = (props) => {
  return <Icon as={CheckLineIcon} {...props} />;
};
