import { Icon } from "@chakra-ui/icons";

import { ReactComponent as SendPlaneLineIcon } from "../../assets/send-plane-line.svg";

export const SendPlaneLine = (props) => {
  return <Icon as={SendPlaneLineIcon} {...props} />;
};
