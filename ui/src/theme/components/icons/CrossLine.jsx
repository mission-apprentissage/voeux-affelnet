import { Icon } from "@chakra-ui/icons";
import { ReactComponent as CrossLineIcon } from "../../assets/cross-line.svg";

export const CrossLine = (props) => {
  return <Icon as={CrossLineIcon} {...props} />;
};
